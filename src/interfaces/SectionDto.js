export default class SectionsDto {
  constructor(className, id, icon, title, content) {
    this.className = className;
    this.id = id;
    this.icon = icon;
    this.title = title;
    this.content = content;
  }
}